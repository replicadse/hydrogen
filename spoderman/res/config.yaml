version: 0.1.0

server:
    address: "0.0.0.0:8080"

redis:
    endpoint: "redis://redis:6379"

# authorizer:
#     endpoint: "http://localhost:8090"
#     headers:
#         Authorization: boosker_auth

routes:
    # connect:
    #     endpoint: "http://localhost:8091"
    #     headers:
    #         Authorization: boosker_cennect
    # disconnect:
    #     endpoint: "http://localhost:8093"
    #     headers:
    #         Authorization: boosker_disconnect
    rules_engine:
        endpoint: "http://rulesengine:8080"
        headers:
            Authorization: boosker_rules_engine
